# novTo-Do List

Приложение для списка дел с Firebase авторизацией, Firestore базой данных и FCM уведомлениями.

## Стек
- **Kotlin** + Jetpack Compose
- **Firebase Auth** — вход по email/паролю
- **Firestore** — синхронизация задач между устройствами в реальном времени
- **FCM** — push-уведомления о выполнении задач
- **Hilt** — DI
- **Navigation Compose** — навигация

## Функции
- Вход / регистрация / сброс пароля
- Главная: задачи на сегодня с поиском
- Все задачи: все дни, фильтрация, быстрое удаление
- Профиль: аватар, настройки, выход
- О приложении
- Синхронизация между устройствами (Firestore realtime)
- Уведомление когда другой пользователь выполнил задачу

## Сборка
GitHub Actions автоматически собирает APK при каждом push в `main`.

**Требования:**
- Android 8.0+ (API 26)

## Установка google-services.json
Положите файл `google-services.json` (из Firebase Console) в папку `app/`.

---

## ⚠️ ВАЖНО: Деплой правил Firestore

Ошибка **"PERMISSION_DENIED: Missing or insufficient permissions"** при создании задач возникает
потому что правила безопасности Firestore **не задеплоены** в Firebase.

Чтобы исправить — выполни одну команду:

```bash
# Установить Firebase CLI (если не установлен)
npm install -g firebase-tools

# Войти в аккаунт
firebase login

# Задеплоить правила Firestore
firebase deploy --only firestore:rules
```

После этого создание задач заработает.

---

## Исправления (v1.0.1)

- **Исправлен баг наложения текста** на экране "О приложении" — строки карточек накладывались
  друг на друга из-за отсутствия `Column` внутри `Surface`
- **Улучшена обработка ошибок** — ошибки Firestore переведены на русский язык
- **Добавлена проверка авторизации** в `TaskRepository` перед записью в Firestore
- **Улучшена иконка приложения** — добавлен неоновый градиент на фоне
